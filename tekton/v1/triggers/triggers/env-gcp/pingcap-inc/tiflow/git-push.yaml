apiVersion: triggers.tekton.dev/v1beta1
kind: Trigger
metadata:
  name: git-push-pingcap-inc-tiflow-linux
  labels:
    type: github-branch-push
    github-owner: pingcap-inc
    github-repo: tiflow
spec:
  interceptors:
    - name: filter on repo owner and name and branches
      ref: { name: cel }
      params:
        - name: filter
          value: >-
            body.repository.full_name == 'pingcap-inc/tiflow'
            &&
            body.ref.matches('^refs/heads/feature/release-8[.]5[.]5-active-active$')
  bindings:
    - ref: github-branch-push
    - ref: gcp-internal-build-params
    - { name: component, value: $(body.repository.name) }
    - { name: profile, value: release }
    - { name: timeout, value: 60m }
    - { name: source-ws-size, value: 50Gi }
    - { name: builder-resources-memory, value: 16Gi }
    - { name: builder-resources-cpu, value: "4" }
  template:
    ref: build-component-linux
