apiVersion: triggers.tekton.dev/v1beta1
kind: Trigger
metadata:
  name: github-tag-build-ng
  labels:
    type: github-tag-create
    profile: nextgen
spec:
  interceptors:
    - name: filter on repo and git tags
      ref: { name: cel }
      params:
        - name: filter
          # support tags:
          # - legacy nextgen tags: v8.5.4-nextgen.202510.1
          # - calendar-semver nextgen tags: v26.3.0, v26.3.1
          value: >-
            body.repository.full_name in [
            'pingcap/ticdc',
            'pingcap/tidb',
            'pingcap/tiflash',
            'pingcap/tiflow',
            'pingcap/tiproxy',
            'tidbcloud/cloud-storage-engine',
            'tikv/pd',
            ]
            &&
            body.ref.matches('^(v[0-9]+[.][0-9]+[.][0-9]+-nextgen[.][0-9]{6}[.][0-9]+|v[1-9][0-9]+[.][0-9]+[.][0-9]+)$')
        - name: overlays
          value:
            - key: resource-config
              expression: >-
                {
                  'pingcap/tiproxy': {
                    'timeout': '30m',
                    'sourceWsSize': '20Gi',
                    'cpu': '3',
                    'memory': '12Gi',
                  },
                  'pingcap/ticdc': {
                    'timeout': '30m',
                    'sourceWsSize': '50Gi',
                    'cpu': '3',
                    'memory': '12Gi',
                  },
                  'pingcap/tidb': {
                    'timeout': '45m',
                    'sourceWsSize': '50Gi',
                    'cpu': '6',
                    'memory': '24Gi',
                  },
                  'pingcap/tiflash': {
                    'timeout': '2h',
                    'sourceWsSize': '100Gi',
                    'cpu': '12',
                    'memory': '48Gi',
                  },
                  'pingcap/tiflow': {
                    'timeout': '40m',
                    'sourceWsSize': '50Gi',
                    'cpu': '6',
                    'memory': '24Gi',
                  },
                  'tikv/pd': {
                    'timeout': '20m',
                    'sourceWsSize': '50Gi',
                    'cpu': '6',
                    'memory': '24Gi',
                  },
                  'tidbcloud/cloud-storage-engine': {
                    'timeout': '30m',
                    'sourceWsSize': '100Gi',
                    'cpu': '12',
                    'memory': '48Gi',
                  },
                }[body.repository.full_name]
            - key: component
              expression: >-
                body.repository.full_name == 'tidbcloud/cloud-storage-engine'
                ?
                'tikv'
                :
                body.repository.name
            - key: profile
              expression: >-
                body.repository.full_name == 'pingcap/tiproxy' ? 'release' : 'nextgen'

  bindings:
    - ref: github-tag-create
    - ref: gcp-ng-build-params
    - { name: component, value: $(extensions.component) }
    - { name: profile, value: $(extensions.profile) }
    - { name: os, value: linux }

  template:
    ref: build-component-linux
