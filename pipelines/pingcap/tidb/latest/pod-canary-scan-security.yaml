apiVersion: v1
kind: Pod
spec:
  securityContext:
    fsGroup: 1000
  containers:
    - name: deno
      image: hub.pingcap.net/jenkins/python3-requests:latest
      tty: true
      resources:
        limits:
          memory: 256Mi
          cpu: 100m
      env:
        - name: TOKEN
          valueFrom:
            secretKeyRef:
              key: token
              name: security-server-api-token

